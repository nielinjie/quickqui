implementations:
  - name: web-front-npm
    abstract: true
    runtime: command
    parameters:
      #NOTE 应该仅用于开发。
      command: "npm"
      args: ["run", "start-without-clean"]
      packageName: "@quick-qui/front3"