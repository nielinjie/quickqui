implementations:
  - name: web-front-raw
    abstract: true
    runtime: command
    parameters:
      #NOTE 应该仅用于开发。
      path: "front"
      command: "npm"
      args: ["run", "start-without-clean"]