implementations:
  - name: web-front-raw
    abstract: true
    runtime: command
    injections: [dataProvider, env]
    parameters:
      #NOTE 应该仅用于开发。
      path: "front3"
      command: "npm"
      args: ["run", "start-without-clean"]
